<!DOCTYPE html>
<html lang="en">
    <head>
        <title><%= title %></title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="css/style.css" rel="stylesheet">
    </head>
    <body>
        <h1>Number Of Records: <%= numberOfRecords %></h1>
        <ul>
        <% for(var i=0; i< results.length; i++) {%>    
            <li>Record title:<%= results[i].recordTitle %> of Record Id:<%= results[i].recordId %></li>
        <% } %>
        </ul>

        <!-- NOTE ilker using new ES5 forEach below, instead of conventional for loop of above -->
        <h1>Number Of Records: <%= numberOfRecords %></h1>
        <ul>
        <% results.forEach(function(result, index) { %>    
            <li>Index: <%= i %> with Record title:<%= result.recordTitle %> of Record Id:<%= result.recordId %></li>
        <% }); %>
        </ul>
    </body>
</html>